# Profile assets

Put ordinary files and directory trees below this directory. Reference them
from `profile.yml` with paths such as `assets/files/gitconfig`. Machstrap
rejects symbolic links and any reference that escapes the profile directory.
